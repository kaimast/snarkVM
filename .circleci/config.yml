version: 2.1

parameters:
  small:
    type: string
    default: small
  medium:
    type: string
    default: medium
  large:
    type: string
    default: large
  xlarge:
    type: string
    default: xlarge
  twoxlarge:
    type: string
    default: 2xlarge

orbs:
  windows: circleci/windows@5.0

executors:
  # The default docker image used in the main-workflow
  rust-docker:
    docker:
      - image: cimg/rust:1.92.0 # Attention - Change the MSRV in Cargo.toml and rust-toolchain as well
    environment:
      RUSTFLAGS: "" # override the config.toml flags

commands:
  check_windows:
    description: "Runs cargo check on Windows"
    parameters:
      workspace_member:
        type: string
    steps:
      - checkout
      - restore_cache:
          keys:
            - cargo-cache-{{ arch }}-{{ checksum "Cargo.lock" }}
      - run:
          name: "Install Rust"
          no_output_timeout: 15m
          command: |
            $ErrorActionPreference = "Stop"
            $ProgressPreference = "SilentlyContinue"

            # Ensure rustup is installed
            if (-not (Get-Command rustup -ErrorAction SilentlyContinue)) {
                Write-Host "rustup not found, installing..."
                $rustupPath = "$env:TEMP\rustup-init.exe"
                Invoke-WebRequest -Uri "https://win.rustup.rs/" -OutFile $rustupPath
                & $rustupPath -y --default-toolchain "1.92.0-x86_64-pc-windows-msvc" --no-modify-path --profile minimal
            } else {
                Write-Host "rustup already installed, ensuring toolchain 1.92.0-x86_64-pc-windows-msvc is present..."
                rustup toolchain install 1.92.0-x86_64-pc-windows-msvc
            }

            # Put cargo/rustup first in PATH for this step and subsequent steps
            $Env:Path = "$Env:USERPROFILE\.cargo\bin;$Env:Path"

            # Select the toolchain
            rustup default 1.92.0-x86_64-pc-windows-msvc

            # Verify the installation.
            cargo --version --verbose
            rustc --version | Out-File -FilePath "rust-version"

            if (!(Test-Path "Cargo.lock" -PathType Leaf)) {
                cargo generate-lockfile
            }
      - run:
          name: "Run cargo check"
          command: |
            # Add cargo to PATH (for safety in case env is not carried perfectly)
            $Env:Path = "$Env:USERPROFILE\.cargo\bin;$Env:Path"

            # Check that the given package compiles
            cd << parameters.workspace_member >>
            cargo check --examples --benches --tests
      - save_cache:
          paths:
            - C:\Users\circleci\.cargo\registry
            - C:\Users\circleci\.cargo\git
          key: cargo-cache-{{ arch }}-{{ checksum "Cargo.lock" }}

  setup_environment:
    description: "Setup testing environment"
    parameters:
      cache_key:
        type: string
        default: v4.6.0-rust-1.92.0-snarkvm-cache
    steps:
      - run:
          name: Prepare environment variables and install dependencies
          command: |
            # Make sure our local bin and cargo bin are on PATH
            echo 'export PATH="$HOME/.cargo/bin:$CIRCLE_WORKING_DIRECTORY/.bin:$PATH"' >> "$BASH_ENV"

            # sccache settings
            echo 'export SCCACHE_DIR="$HOME/.cache/sccache"' >> "$BASH_ENV"
            echo 'export SCCACHE_CACHE_SIZE="2000M"' >> "$BASH_ENV"
            echo 'export SCCACHE_IDLE_TIMEOUT=0' >> "$BASH_ENV"
            echo 'export SCCACHE_LOG=info' >> "$BASH_ENV"

            # Disable incremental builds so sccache behaves nicely
            echo 'export CARGO_INCREMENTAL=0' >> "$BASH_ENV"

            # Use sccache for *all* Rust compile commands.
            echo 'export RUSTC_WRAPPER="sccache"' >> "$BASH_ENV"
      - run:
          name: Install sccache
          command: |
            export WORK_DIR="$HOME/.cache/sccache"
            export SCCACHE_DIR="$HOME/.cache/sccache"
            export SCCACHE_VERSION=v0.10.0
            export SCCACHE_PKG="sccache-$SCCACHE_VERSION-x86_64-unknown-linux-musl"

            mkdir -p "$CIRCLE_WORKING_DIRECTORY/.bin"
            mkdir -p "$HOME/.cache/sccache"
            if [ ! -x "$CIRCLE_WORKING_DIRECTORY/.bin/sccache" ]; then
              wget "https://github.com/mozilla/sccache/releases/download/$SCCACHE_VERSION/$SCCACHE_PKG.tar.gz"
              tar -C "$CIRCLE_WORKING_DIRECTORY/.bin" -xvf "$SCCACHE_PKG.tar.gz"
              chmod +x "$CIRCLE_WORKING_DIRECTORY/.bin/$SCCACHE_PKG/sccache"
              mv "$CIRCLE_WORKING_DIRECTORY/.bin/$SCCACHE_PKG/sccache" "$CIRCLE_WORKING_DIRECTORY/.bin/sccache"
              rm -rf "$CIRCLE_WORKING_DIRECTORY/.cargo/registry"
            fi
      - run:
          name: Install debian packages
          command: |
            DEBIAN_FRONTEND=noninteractive sudo apt-get update
            DEBIAN_FRONTEND=noninteractive sudo apt-get install -y --no-install-recommends \
              clang llvm-dev llvm lld pkg-config xz-utils make libssl-dev libssl-dev
      - restore_cache:
          keys:
            - v4.6.0-rust-1.92.0-deps-{{ checksum "Cargo.lock" }}-
            - v4.6.0-rust-1.92.0-deps-
      - restore_cache:
          keys:
            - << parameters.cache_key >>

  clear_environment:
    description: "Clear environment"
    parameters:
      cache_key:
        type: string
        default: v4.6.0-rust-1.92.0-snarkvm-cache
    steps:
      - run: set +e
      - save_cache:
          key: v4.6.0-rust-1.92.0-deps-{{ checksum "Cargo.lock" }}
          paths:
            - /home/circleci/.cache/sccache
            - /home/circleci/.cargo/registry
            - /home/circleci/.rustup
            - /home/circleci/.cargo/bin
            - /home/circleci/project/.bin
      - save_cache:
          key: << parameters.cache_key >>
          paths:
            - /home/circleci/.aleo/resources

  run_test:
    description: "Build and run tests"
    parameters:
      workspace_member:
        type: string
      flags:
        type: string
        default: ""
      cache_key_suffix:
        type: string
        default: ""
      timeout:
        type: string
        default: 10m
      no_output_timeout:
        type: string
        default: 10m
      use_cache:
        type: boolean
        default: true
    steps:
      - checkout

      - when:
          condition: << parameters.use_cache >>
          steps:
            - setup_environment:
                cache_key: v4.6.0-rust-1.92.0-<< parameters.workspace_member >><< parameters.cache_key_suffix >>-cache

      - run:
          name: Install debian packages (always)
          command: |
            DEBIAN_FRONTEND=noninteractive sudo apt-get update
            DEBIAN_FRONTEND=noninteractive sudo apt-get install -y --no-install-recommends \
              clang llvm-dev llvm lld pkg-config xz-utils make libssl-dev

      - run:
          name: "No-cache mode: clean + disable sccache"
          command: |
            set -euo pipefail
            USE_CACHE="<< parameters.use_cache >>"

            if [ "$USE_CACHE" != "true" ]; then
              echo "No-cache mode: cleaning and disabling sccache"
              rm -rf target ~/.cache/sccache ~/.cargo/registry ~/.cargo/git || true

              # Make sure later steps don't accidentally use sccache
              echo 'unset RUSTC_WRAPPER' >> "$BASH_ENV"
              echo 'unset SCCACHE_DIR' >> "$BASH_ENV"
              echo 'unset SCCACHE_CACHE_SIZE' >> "$BASH_ENV"
              echo 'unset SCCACHE_IDLE_TIMEOUT' >> "$BASH_ENV"
              echo 'unset SCCACHE_LOG' >> "$BASH_ENV"

              echo 'export PATH="$HOME/.cargo/bin:$PATH"' >> "$BASH_ENV"

              # If anything tries to call it directly
              rm -rf "$CIRCLE_WORKING_DIRECTORY/.bin/sccache" || true
            else
              echo "Cached mode: sccache enabled by setup_environment"
            fi

      - run:
          name: Install cargo-nextest
          command: |
            set -euo pipefail
            BIN_ROOT="$CIRCLE_WORKING_DIRECTORY/.bin"
            mkdir -p "$BIN_ROOT"

            export PATH="$HOME/.cargo/bin:$PATH"
            if ! command -v cargo-nextest >/dev/null 2>&1; then
              cargo install cargo-nextest --locked --version "0.9.114"
            fi

      - run:
          name: Debug rustc wrapper
          command: |
            set -euo pipefail
            USE_CACHE="<< parameters.use_cache >>"

            echo "USE_CACHE=$USE_CACHE"
            echo "RUSTC_WRAPPER=${RUSTC_WRAPPER:-<unset>}"
            which rustc || echo "rustc not found on PATH"

            if [ "$USE_CACHE" = "true" ]; then
              which sccache || echo "sccache not found on PATH"
            else
              echo "sccache intentionally disabled"
            fi

      - run:
          name: "Run Tests"
          timeout: << parameters.timeout >>
          no_output_timeout: << parameters.no_output_timeout >>
          command: |
            set -euo pipefail
            export CARGO_TERM_COLOR=never
            USE_CACHE="<< parameters.use_cache >>"

            echo "[debug] USE_CACHE=$USE_CACHE"
            echo "[debug] PATH=$PATH"
            echo "[debug] RUSTC_WRAPPER=${RUSTC_WRAPPER:-<unset>}"
            which rustc || echo "[debug] rustc not found on PATH"
            if [ "$USE_CACHE" = "true" ]; then
              which sccache || echo "[debug] sccache not found on PATH"
              echo "[debug] SCCACHE_DIR=${SCCACHE_DIR:-<unset>}"
              echo "[debug] SCCACHE_LOG=${SCCACHE_LOG:-<unset>}"
              echo "[debug] SCCACHE_IDLE_TIMEOUT=${SCCACHE_IDLE_TIMEOUT:-<unset>}"

              "$CIRCLE_WORKING_DIRECTORY/.bin/sccache" --zero-stats || true
              echo "[sccache stats BEFORE nextest]"
              "$CIRCLE_WORKING_DIRECTORY/.bin/sccache" -s || true
            else
              echo "[debug] sccache disabled for this run"
            fi

            # Create artifact + nextest output directories
            mkdir -p artifacts target/nextest/ci

            # Create artifact + nextest output directories
            OUT="artifacts/<< parameters.workspace_member >>-nextest.txt"
            CSV="artifacts/<< parameters.workspace_member >>-test-times.csv"
            TOP="artifacts/<< parameters.workspace_member >>-slow-tests-top30.csv"
            JUNIT="target/nextest/ci/junit.xml"

            FLAGS_FULL="<< parameters.flags >>"
            FLAGS_TRIM="$(printf '%s' "$FLAGS_FULL" | sed -e 's/^[[:space:]]*//' -e 's/[[:space:]]*$//')"

            RIGHT_OF_SEP=""
            if printf '%s' "$FLAGS_TRIM" | grep -q ' -- '; then
              BUILD_FLAGS="${FLAGS_TRIM%% -- *}"
              RIGHT_OF_SEP="${FLAGS_TRIM#* -- }"
            elif printf '%s' "$FLAGS_TRIM" | grep -Eq '^--[[:space:]]'; then
              BUILD_FLAGS=""
              RIGHT_OF_SEP="${FLAGS_TRIM#-- }"
            else
              BUILD_FLAGS="$FLAGS_TRIM"
            fi

            WSM="<< parameters.workspace_member >>"

            # Remove any --test target flags,they cause nextest build errors
            BUILD_FLAGS_SANITIZED="$(printf '%s' "$BUILD_FLAGS" \
              | sed -E "s/(^|[[:space:]])--test[[:space:]]+'[^']*'([[:space:]]|$)/ /g" \
              | sed -E 's/(^|[[:space:]])--test[[:space:]]+\"[^\"]*\"([[:space:]]|$)/ /g' \
              | sed -E 's/(^|[[:space:]])--test[[:space:]]+[^[:space:]]+([[:space:]]|$)/ /g' \
              | tr -s ' ' | sed -E 's/^[[:space:]]+//; s/[[:space:]]+$//' \
            )"

            # Detect wildcard integration selection: `--test '*'` (with or without quotes)
            HAS_TEST_WILDCARD=0
            if printf '%s\n' "$BUILD_FLAGS" | grep -Eq -- '--test[[:space:]]+("\*"|'\''\*'\''|\*)'; then
              HAS_TEST_WILDCARD=1
            fi

            # Build nextest filter expression from any --skip <pattern> arguments
            FILTER_EXPR=""
            if [ "$WSM" = "snarkvm-synthesizer" ] && [ "$HAS_TEST_WILDCARD" -eq 1 ]; then
              FILTER_EXPR='test(/test_command_parse/)+test(/test_instruction_parse/)+test(/test_process_execute/)+test(/test_program_parse/)+test(/test_vm_execute_and_finalize/)'
            fi

            # Build nextest filter expression from any --skip <pattern> arguments
            if [ -z "$FILTER_EXPR" ] && [ -n "$RIGHT_OF_SEP" ]; then
              SKIPS="$(printf '%s\n' "$RIGHT_OF_SEP" \
                | sed -E 's/[[:space:]]+/ /g' \
                | grep -oE -- '--skip[[:space:]]+(\"[^\"]+\"|'\''[^'\'']+'\''|[^[:space:]]+)' || true)"
              if [ -n "$SKIPS" ]; then
                EXPRS=""
                tmpf="$(mktemp)"
                printf '%s\n' "$SKIPS" > "$tmpf"
                while IFS= read -r line; do
                  pat="${line#--skip }"
                  pat="${pat%\"}"; pat="${pat#\"}"
                  pat="${pat%\'}"; pat="${pat#\'}"
                  pat_rx="${pat//\//\\/}"
                  if [ -z "$EXPRS" ]; then
                    EXPRS="test(/$pat_rx/)"
                  else
                    EXPRS="$EXPRS or test(/$pat_rx/)"
                  fi
                done < "$tmpf"
                rm -f "$tmpf"
                if [ -n "$EXPRS" ]; then
                  FILTER_EXPR="not ($EXPRS)"
                fi
              fi
            fi

            # Detect and extract --test-threads value (used to set JOBS)
            TEST_THREADS=""
            if [ -n "$RIGHT_OF_SEP" ]; then
              TEST_THREADS="$(printf '%s\n' "$RIGHT_OF_SEP" | sed -nE 's/.*--test-threads=([0-9]+).*/\1/p' | head -n1)"
              if [ -z "$TEST_THREADS" ]; then
                TEST_THREADS="$(printf '%s\n' "$RIGHT_OF_SEP" | sed -nE 's/.*--test-threads[[:space:]]+([0-9]+).*/\1/p' | head -n1)"
              fi
            fi

            DEFAULT_JOBS=8
            JOBS="${NEXTEST_JOBS:-$DEFAULT_JOBS}"
            if [ -n "$TEST_THREADS" ]; then
              JOBS="$TEST_THREADS"
            fi

            # Create nextest config (disable fail-fast, add slow-timeout + JUnit output)
            SLOW_PERIOD="${NEXTEST_SLOW_PERIOD:-60s}"
            CFG_ARGS=()
            TMPD="$(mktemp -d)"
            if [ -f ".config/nextest.toml" ]; then
              CFG_ARGS+=(--config-file ".config/nextest.toml")
            fi
            {
              printf '%s\n' '[profile.ci]'
              printf '%s\n' 'fail-fast = false'
              printf '%s\n' "slow-timeout = \"${SLOW_PERIOD}\""
              printf '%s\n' ''
              printf '%s\n' '[profile.ci.junit]'
              printf '%s\n' 'path = "junit.xml"'
            } > "$TMPD/nextest-override.toml"
            CFG_ARGS+=(--config-file "$TMPD/nextest-override.toml")

            # Print configuration summary for debugging
            echo "==> nextest package: << parameters.workspace_member >>"
            echo "==> build flags (sanitized): '${BUILD_FLAGS_SANITIZED}'"
            echo "==> filter-expr: ${FILTER_EXPR:-<none>}  (right-of-sep: ${RIGHT_OF_SEP:-<none>})"
            echo "==> test-threads override: ${TEST_THREADS:-<none>}"
            echo "==> jobs: ${JOBS}"

            # Use plain cargo test if --no-run was requested
            # Otherwise, run tests with cargo nextest
            if printf '%s' " $BUILD_FLAGS_SANITIZED " | grep -q ' --no-run '; then
              RUST_MIN_STACK=67108864 cargo test --package="<< parameters.workspace_member >>" $BUILD_FLAGS_SANITIZED | tee "$OUT"
              : > "$CSV"; : > "$TOP"
            else
              rm -rf target
              RUST_MIN_STACK=67108864 \
              cargo nextest run \
                -p "<< parameters.workspace_member >>" $BUILD_FLAGS_SANITIZED \
                --profile ci -j "${JOBS}" "${CFG_ARGS[@]}" \
                --status-level fail --hide-progress-bar \
                --no-tests=pass \
                ${FILTER_EXPR:+--filter-expr "$FILTER_EXPR"} \
                2>&1 | tee "$OUT"

              if [ -f "$JUNIT" ]; then
                awk -v RS='<testcase ' '
                  NR>1 {
                    name=""; classname=""; time="";
                    start=index($0,"classname=\""); if(start>0){ start+=11; rest=substr($0,start); end=index(rest,"\""); if(end>0) classname=substr(rest,1,end-1) }
                    start=index($0,"name=\"");      if(start>0){ start+=6;  rest=substr($0,start); end=index(rest,"\""); if(end>0) name=substr(rest,1,end-1) }
                    start=index($0,"time=\"");      if(start>0){ start+=6;  rest=substr($0,start); end=index(rest,"\""); if(end>0) time=substr(rest,1,end-1) }
                    if(name!="" && time!=""){ full=(classname!=""?classname"::"name:name); printf "%s,%s\n", full, time }
                  }
                ' "$JUNIT" > "$CSV" || true

                sort -t, -k2,2nr "$CSV" | head -n 30 > "$TOP" || true
              else
                echo "WARN: $JUNIT not found; no timings extracted." >&2
                : > "$CSV"; : > "$TOP"
              fi
            fi

            if [ "$USE_CACHE" = "true" ]; then
              echo "[sccache stats AFTER nextest]"
              "$CIRCLE_WORKING_DIRECTORY/.bin/sccache" -s || true
            fi

      - store_artifacts:
          path: artifacts
          destination: test-timings/<< parameters.workspace_member >>
      - store_test_results:
          path: target/nextest/ci

      - when:
          condition: << parameters.use_cache >>
          steps:
            - clear_environment:
                cache_key: v4.6.0-rust-1.92.0-<< parameters.workspace_member >><< parameters.cache_key_suffix >>-cache

  install_rust_nightly:
    description: "Install Rust nightly toolchain"
    steps:
      - run: rustup toolchain install nightly-x86_64-unknown-linux-gnu

jobs:
  snarkvm:
    executor: rust-docker
    resource_class: << pipeline.parameters.large >>
    steps:
      - checkout
      - run_test:
          workspace_member: snarkvm

  algorithms:
    executor: rust-docker
    resource_class: << pipeline.parameters.large >>
    steps:
      - run_test:
          workspace_member: snarkvm-algorithms

  circuit:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit

  circuit-account:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit-account

  circuit-algorithms:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit-algorithms

  circuit-collections:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit-collections

  circuit-environment:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit-environment

  circuit-network:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit-network

  circuit-program:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit-program

  circuit-types:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit-types

  circuit-types-address:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit-types-address

  circuit-types-boolean:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit-types-boolean

  circuit-types-field:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit-types-field

  circuit-types-group:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit-types-group

  circuit-types-integers:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit-types-integers
          flags: -- --ignored

  circuit-types-scalar:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit-types-scalar

  circuit-types-string:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-circuit-types-string

  console:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-console

  console-account:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - run_test:
          workspace_member: snarkvm-console-account

  console-algorithms:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-console-algorithms

  console-collections:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-console-collections

  console-network:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-console-network

  console-network-environment:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-console-network-environment

  console-program:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-console-program

  console-types:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-console-types

  console-types-address:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-console-types-address

  console-types-boolean:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-console-types-boolean

  console-types-field:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-console-types-field

  console-types-group:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-console-types-group

  console-types-integers:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-console-types-integers

  console-types-scalar:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-console-types-scalar

  console-types-string:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-console-types-string

  curves:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-curves

  fields:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-fields

  ledger:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger

  ledger-with-rocksdb-partition1:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger
          flags: --release --features=rocks --partition count:1/2 -- --test-threads=10
          no_output_timeout: 20m
          timeout: 30m

  ledger-with-rocksdb-partition2:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger
          flags: --release --features=rocks --partition count:2/2 -- --test-threads=10
          no_output_timeout: 20m
          timeout: 30m

  ledger-with-valid-solutions:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger
          cache_key_suffix: -with-vaild-solutions
          flags: valid_solutions --features=test

  ledger-with-valid-solutions-prerelease:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger
          cache_key_suffix: -with-vaild-solutions-prerelease
          flags: valid_solutions --features test --run-ignored ignored-only

  ledger-authority:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-authority

  ledger-block:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-block
          no_output_timeout: 20m
          timeout: 30m

  ledger-committee:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-committee

  ledger-narwhal:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-narwhal

  ledger-narwhal-batch-certificate:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-narwhal-batch-certificate

  ledger-narwhal-batch-header:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-narwhal-batch-header

  ledger-narwhal-data:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-narwhal-data

  ledger-narwhal-subdag:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-narwhal-subdag

  ledger-narwhal-transmission:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-narwhal-transmission

  ledger-narwhal-transmission-id:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-narwhal-transmission-id

  ledger-puzzle:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-puzzle

  ledger-puzzle-epoch:
    executor: rust-docker
    resource_class: << pipeline.parameters.large >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-puzzle-epoch

  ledger-query:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-query

  ledger-store:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-store
          flags: --features=rocks

  ledger-store-prerelease:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-store
          flags: --run-ignored ignored-only -- --test-threads 2
          cache_key_suffix: -ledger-store-prerelease

  ledger-test-helpers:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - run_test:
          workspace_member: snarkvm-ledger-test-helpers

  parameters:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - run_test:
          timeout: 20m
          workspace_member: snarkvm-parameters
          flags: -- --test-threads=2

  parameters-large:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-parameters
          flags: --no-run --features large_params
          use_cache: false
          no_output_timeout: 20m
          timeout: 30m

  parameters-uncached:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-parameters
          flags: --run-ignored ignored-only -- --test-threads=2 
          use_cache: false
          cache_key_suffix: -v-{{ epoch }}
          no_output_timeout: 20m
          timeout: 30m

  synthesizer:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer
          flags: --lib --bins -- --test-threads 16
          no_output_timeout: 20m
          timeout: 30m

  synthesizer-prerelease:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer
          flags: --features test --run-ignored ignored-only -- --test-threads 2
          no_output_timeout: 20m
          cache_key_suffix: -synthesizer-prerelease
          timeout: 20m

  synthesizer-test-partition1:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer
          flags: >
            --lib --bins --features test
            --partition count:1/2
            -- --test-threads 8
          cache_key_suffix: -test1
          timeout: 25m

  synthesizer-test-partition2:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer
          flags: >
            --lib --bins --features test
            --partition count:2/2
            -- --test-threads 8
          cache_key_suffix: -test2
          timeout: 25m

  synthesizer-integration:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - run_test:
          timeout: 30m # test_vm_execute_and_finalize can take over 10 minutes in the current setup
          no_output_timeout: 20m
          workspace_member: snarkvm-synthesizer
          flags: --test '*' --features test,dev-print -- --test-threads=4
          cache_key_suffix: -integration

  synthesizer-process:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer-process

  synthesizer-process-with-rocksdb:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer-process
          flags: --features=rocks -- --test-threads=4
          cache_key_suffix: -with-rocksdb

  synthesizer-program:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer-program
          flags: --lib --bins

  synthesizer-program-integration:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer-program
          flags: -- --skip keccak --skip psd --skip sha --skip instruction::is --skip instruction::equal --skip instruction::commit --test-threads=8
          cache_key_suffix: -integration

  synthesizer-program-integration-keccak:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer-program
          flags: keccak -- --test-threads=4
          timeout: 30m
          no_output_timeout: 20m
          cache_key_suffix: -integration-keccak

  synthesizer-program-integration-psd:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer-program
          flags: psd
          cache_key_suffix: -integration-psd

  synthesizer-program-integration-sha:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer-program
          flags: sha -- --test-threads=4
          timeout: 30m
          no_output_timeout: 20m
          cache_key_suffix: -integration-sha

  synthesizer-program-integration-instruction-is:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer-program
          flags: instruction::is
          cache_key_suffix: -integration-is

  synthesizer-program-integration-instruction-equal:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer-program
          flags: instruction::equal
          cache_key_suffix: -integration-equal

  synthesizer-program-integration-instruction-commit:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer-program
          flags: instruction::commit
          cache_key_suffix: -integration-commit

  synthesizer-snark:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-synthesizer-snark

  utilities:
    executor: rust-docker
    resource_class: << pipeline.parameters.large >>
    steps:
      - run_test:
          workspace_member: snarkvm-utilities

  utilities-derives:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - run_test:
          workspace_member: snarkvm-utilities-derives

  wasm:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - checkout
      - setup_environment:
          cache_key: v4.6.0-rust-1.92.0-snarkvm-wasm-cache
      - run:
          no_output_timeout: 30m
          command: |
            # The commands required to install current Node.js come from https://nodejs.org/en/download
            # The versions of nvm and Node.js may need to be periodically updated
            # Download and install nvm
            curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
            
            # Add nvm to the path
            source /home/circleci/.profile
            
            # Download and install Node.js
            nvm install 22
            
            # Install wasm-pack
            curl https://rustwasm.github.io/wasm-pack/installer/init.sh -sSf | sh

            # Run the tests
            cd wasm && wasm-pack test --node
      - clear_environment:
          cache_key: v4.6.0-rust-1.92.0-snarkvm-wasm-cache

  check-fmt:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - checkout
      - install_rust_nightly
      - setup_environment:
          cache_key: v4.6.0-rust-1.92.0-snarkvm-fmt-cache
      - run:
          name: Check style
          no_output_timeout: 35m
          command: cargo +nightly fmt --all -- --check
      - clear_environment:
          cache_key: v4.6.0-rust-1.92.0-snarkvm-fmt-cache

  check-unused-dependencies:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - checkout
      - setup_environment:
          cache_key: v4.6.0-rust-1.92.0-machete-cache
      - run:
          name: Check for unused dependencies
          no_output_timeout: 10m
          command: |
            set -euo pipefail
            if ! command -v cargo-machete >/dev/null 2>&1; then
              cargo install cargo-machete@0.7.0 --locked
            fi
            cargo machete
      - clear_environment:
          cache_key: v4.6.0-rust-1.92.0-machete-cache

  check-cargo-audit:
    executor: rust-docker
    resource_class: << pipeline.parameters.medium >>
    steps:
      - checkout
      - setup_environment:
          cache_key: v4.6.0-rust-1.92.0-cargo-audit-cache
      - run:
          name:  Check for security vulnerabilities
          no_output_timeout: 10m
          command: |
            cargo install cargo-audit@0.22.0 --locked --force
            cargo audit -D warnings
      - clear_environment:
          cache_key: v4.6.0-rust-1.92.0-cargo-audit-cache

  check-cargo-semver-checks:
    executor: rust-docker
    resource_class: << pipeline.parameters.twoxlarge >>
    steps:
      - checkout:
          method: full
      - setup_environment:
          cache_key: v4.6.0-rust-1.92.0-cargo-semver-checks-cache
      - run:
          name: Check for semver violations
          no_output_timeout: 15m
          command: |
            bash ./.circleci/semver-checks.sh
      - clear_environment:
          cache_key: v4.6.0-rust-1.92.0-cargo-semver-checks-cache

  check-clippy:
    executor: rust-docker
    resource_class: << pipeline.parameters.xlarge >>
    steps:
      - checkout
      - setup_environment:
          cache_key: v4.6.0-rust-1.92.0-snarkvm-clippy-cache
      - run:
          name: Check Clippy (default features)
          timeout: 20m
          command: |
            cargo clippy --workspace --all-targets -- -D warnings
      - run:
          name: Check Clippy (all features)
          timeout: 20m
          command: |
            cargo clippy --workspace --all-targets --all-features -- -D warnings
      - clear_environment:
          cache_key: v4.6.0-rust-1.92.0-snarkvm-clippy-cache

  check-all-targets:
    executor: rust-docker
    resource_class: << pipeline.parameters.small >>
    steps:
      - checkout
      - setup_environment:
          cache_key: v4.6.0-rust-1.92.0-snarkvm-all-targets-cache
      - run:
          name: Check all targets
          no_output_timeout: 35m
          command: cargo check --release --workspace --all-targets
      - clear_environment:
          cache_key: v4.6.0-rust-1.92.0-snarkvm-all-targets-cache

  verify-windows:
    executor:
      name: windows/default
      size: xlarge
    environment:
      CARGO_NET_GIT_FETCH_WITH_CLI: "true"
      RUSTFLAGS: "" # override the config.toml flags
    parameters:
      workspace_member:
        type: string
    steps:
      - check_windows:
          workspace_member: << parameters.workspace_member >>

workflows:
  version: 2

  cargo-workflow:
    jobs:
      - check-fmt
      - check-clippy
      - check-cargo-audit
      - check-all-targets

  circuit-workflow:
    jobs:
      - circuit
      - circuit-account
      - circuit-algorithms
      - circuit-collections
      - circuit-environment
      - circuit-network
      - circuit-program
      - circuit-types
      - circuit-types-address
      - circuit-types-boolean
      - circuit-types-field
      - circuit-types-group
      - circuit-types-integers
      - circuit-types-scalar
      - circuit-types-string

  console-workflow:
    jobs:
      - console
      - console-account
      - console-algorithms
      - console-collections
      - console-network
      - console-network-environment
      - console-program
      - console-types
      - console-types-address
      - console-types-boolean
      - console-types-field
      - console-types-group
      - console-types-integers
      - console-types-scalar
      - console-types-string

  ledger-workflow:
    jobs:
      - ledger
      - ledger-with-valid-solutions
      - ledger-authority
      - ledger-committee
      - ledger-narwhal
      - ledger-narwhal-batch-certificate
      - ledger-narwhal-batch-header
      - ledger-narwhal-data
      - ledger-narwhal-subdag
      - ledger-narwhal-transmission
      - ledger-narwhal-transmission-id
      - ledger-puzzle
      - ledger-puzzle-epoch
      - ledger-query
      - ledger-store
      - ledger-test-helpers

  synthesizer-workflow:
    jobs:
      - synthesizer
      - synthesizer-process
      - synthesizer-program
      - synthesizer-snark

  snarkvm-workflow:
    jobs:
      - algorithms
      - curves
      - fields
      - snarkvm
      - utilities
      - utilities-derives
      - wasm

  # Run additional but unlikely to fail checks on merges only
  merge-workflow:
    when: pipeline.git.branch == "staging"
      or pipeline.git.branch == "canary"
      or pipeline.git.branch == "testnet"
      or pipeline.git.branch == "mainnet"
      or pipeline.git.branch == "mainnet-release-v460"
    jobs:
      - check-unused-dependencies # This can be cleaned up before releases
      - check-cargo-semver-checks # This can be cleaned up before releases
      - ledger-with-rocksdb-partition1 # Often similar results as `ledger`
      - ledger-with-rocksdb-partition2 # Often similar results as `ledger`
      - ledger-block # Very heavy and time taking run
      - ledger-store-prerelease # Run the slowest ledger-store tests as pre-release
      - ledger-with-valid-solutions-prerelease # Run the slowest ledger-with-valid-solution test as pre-release
      - parameters # Infrastructure which often runs fine
      - parameters-large # Infrastructure which often runs fine
      - parameters-uncached # Infrastructure which often runs fine
      - synthesizer-integration # Very slow test taking over 30+ minutes to pass
      - synthesizer-prerelease # Run the slowest synthesizer tests as pre-release
      - synthesizer-process-with-rocksdb # Often similar results as `synthesizer-process`
      - synthesizer-program-integration # Expensive, rarely impacted, duplicate test-runs with `synthesizer`
      - synthesizer-program-integration-keccak # Expensive and rarely impacted
      - synthesizer-program-integration-psd # Expensive and rarely impacted
      - synthesizer-program-integration-sha # Expensive and rarely impacted
      - synthesizer-program-integration-instruction-is # Expensive and rarely impacted
      - synthesizer-program-integration-instruction-equal # Expensive and rarely impacted
      - synthesizer-program-integration-instruction-commit # Expensive and rarely impacted
      - synthesizer-test-partition1 # Often similar results as `synthesizer`
      - synthesizer-test-partition2 # Often similar results as `synthesizer`

  release-workflow:
    when: pipeline.git.branch == "canary"
      or pipeline.git.branch == "testnet"
      or pipeline.git.branch == "mainnet"
    jobs:
      - verify-windows: # Windows has never failed us when linux passed
          matrix:
            parameters:
              workspace_member: [
                algorithms,
                circuit,
                console,
                curves,
                fields,
                ledger,
                parameters,
                synthesizer,
                utilities,
              ]
